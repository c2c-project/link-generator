# GENERATES CSV FILE
import csv
import json

# func for json to csv
# def getItems(jsonObject):
#     return list(jsonObject.items())

# def backup(backupTarget):
#     with open('backup.csv', 'w') as f:
#         headers = list()
#         for key in backupTarget[0]:
#             headers.append(key)
#         csvFile = csv.writer(f)
#         csvFile.writerow(headers)
#         for contact in backupTarget:
            
